using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.UI;

public class TutorialScript : MonoBehaviour
{
    /*
    TutorialString tutorialStr;
    TMPro.TextMeshProUGUI tutorialText;

    CanvasGroup panel;

    private void Start()
    {
        tutorialStr = new TutorialString();
        tutorialText = transform.Find("text").GetComponent<TMPro.TextMeshProUGUI>();
        panel = GetComponent<CanvasGroup>();
    }
    public void ShowTutorialString(int number)
    {

    }

    private IEnumerator OpenPanel()
    {
        isPanelOpen = true;
        while (panelCanvasGroup.alpha < 1f)
        {
            panelCanvasGroup.alpha += transitionSpeed * Time.deltaTime;
            yield return null;
        }
    }

    private IEnumerator ClosePanel()
    {
        isPanelOpen = false;
        while (panelCanvasGroup.alpha > 0f)
        {
            panelCanvasGroup.alpha -= transitionSpeed * Time.deltaTime;
            yield return null;
        }
    }
    */
}
